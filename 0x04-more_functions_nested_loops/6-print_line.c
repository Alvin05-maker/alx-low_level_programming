#include <stdio.h>
#include <unistd.h>
/**
 * print_line - prints line
 * @n - number of times the charcter '_' should be printed
 * return: returns line
 */
void print_line(int n)
{
	int nCopy = n;

	while (nCopy > o)
	{
		putchar('_');
		nCopy--;
	}
	else
	{
		return (\n);
	}
}

